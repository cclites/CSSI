<?php

use Monolog\Logger;
use Monolog\Handler\StreamHandler;

/*
function cLog($logData, $logPath, $type){
	
	//Log::useFiles($path);
	//Log::writeLog($level, $log);
	
	$orderLog = new Logger(order);
    $orderLog->pushHandler(new StreamHandler(storage_path('logs/' . $type . '/' . $logPath . '.log')), Logger::INFO);
	$orderLog->info($type, $log);
}
*/
	
	

